# 🔧 Инструкция по восстановлению системы Python

## ⚠️ Что произошло?

Ваша система Python была изменена предыдущими запусками приложения PANDORA. Скрипты использовали **глобальный Python** вместо изолированной виртуальной окружения, что привело к:

- ❌ Понижению версии `python-multipart` с >=0.0.9 до 0.0.6
- ❌ Конфликтам с другими проектами (особенно gradio)
- ❌ Загрязнению глобальной системной среды

## ✅ Что мы исправили?

Проект PANDORA теперь использует **изолированную виртуальную окружение**:

1. ✅ Создана папка `venv/` внутри проекта
2. ✅ Все зависимости установлены **только** в эту папку
3. ✅ Скрипты `start.py` и `build.py` обновлены для использования venv
4. ✅ Глобальная система Python больше не будет загрязняться

## 🚀 Что делать дальше?

### Вариант 1: Быстрая переинициализация (Рекомендуется)

```powershell
# Перейдите в папку проекта
cd "g:\CODING\PANDORA_FOR_PROMPTS\PANDORA_FOR_PROMPTS-main"

# Переинициализируйте виртуальное окружение (удалит старую, создаст новую)
python reinit_venv.py
```

Это займет 1-2 минуты и создаст чистую виртуальную окружение.

### Вариант 2: Проверить здоровье проекта

```powershell
python health_check.py
```

Эта команда проверит все компоненты проекта и скажет, что нужно сделать.

### Вариант 3: Восстановить глобальный Python (Опционально)

Если у вас есть другие проекты, которые используют `gradio` или другие пакеты:

```powershell
# Обновите python-multipart до нужной версии
pip install --upgrade python-multipart

# Обновите gradio
pip install --upgrade gradio

# Или переустановите нужные пакеты
pip uninstall python-multipart gradio -y
pip install python-multipart gradio
```

## 🎯 Запуск приложения

После восстановления виртуального окружения:

```powershell
# Запустить весь проект (backend + frontend)
python start.py

# Или только backend
python start.py --backend-only

# Пропустить проверки (для скорости)
python start.py --skip-checks
```

## 📊 Проверка статуса

Убедитесь, что всё работает:

```powershell
# Проверить здоровье проекта
python health_check.py

# Проверить, что пакеты в venv
.\venv\Scripts\python.exe -m pip list | findstr "fastapi"

# Проверить глобальный Python (должно быть пусто или минимум)
pip list | findstr "fastapi"
```

## 🔒 Почему это безопаснее?

| До | После |
|----|----|
| Все пакеты в `C:\Users\...\AppData\Local\Programs\Python\...` | Все пакеты в `./venv/Lib/site-packages/` |
| Конфликты с другими проектами | Каждый проект изолирован |
| Сложное удаление | Просто `Remove-Item -Recurse venv` |
| Невозможно иметь разные версии | Каждый проект может иметь своё окружение |

## 📝 Рекомендации на будущее

1. **Всегда используйте виртуальные окружения** для Python проектов
2. **Никогда** не запускайте `pip install` глобально для проектов
3. **PANDORA безопасен** - больше не будет загрязнять систему

## ❓ Часто задаваемые вопросы

### Q: Что произойдёт с моей системой Python?
**A:** Ничего! Новая архитектура использует изолированную venv. Глобальный Python не будет затронут.

### Q: Нужно ли мне переустанавливать Python?
**A:** Нет. Просто запустите `python reinit_venv.py` и готово.

### Q: Могу ли я удалить папку venv?
**A:** Да! Просто удалите папку `venv/` и запустите `python reinit_venv.py` для пересоздания.

### Q: Почему раньше это не было сделано?
**A:** Это была ошибка архитектуры в исходном дизайне. Теперь исправлено.

### Q: Нужно ли обновлять мои другие проекты?
**A:** Если у вас есть другие проекты с gradio - обновите `python-multipart`:
```powershell
pip install --upgrade python-multipart
```

## 🎉 Готово!

Ваш проект PANDORA теперь работает с правильной архитектурой. 

Начните с:
```powershell
python health_check.py
python start.py --backend-only
```

Если у вас есть вопросы, см. другие файлы документации:
- `SYSTEM_RECOVERY.md` - Подробное восстановление
- `docs/ARCHITECTURE_MIGRATION.md` - Технические детали изменений
- `QUICK_START.md` - Быстрый старт
- `SETUP.md` - Полная настройка

**Спасибо за терпение! 🙏**
