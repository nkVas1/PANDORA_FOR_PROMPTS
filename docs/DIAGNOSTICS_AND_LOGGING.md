# 🔧 PANDORA Diagnostics & Logging Guide

## Что было улучшено в Launcher v4.1

### 1. Файловое логирование
✅ Все события теперь логируются в файл для анализа ошибок

**Путь логов:**
```
%APPDATA%\PANDORA\logs\splash.log
```

На Windows это обычно:
```
C:\Users\[YourUsername]\AppData\Roaming\PANDORA\logs\splash.log
```

### 2. Задержка при ошибках
✅ Если произойдет ошибка, splash screen будет ждать **10 секунд** перед закрытием

**Это дает время на:**
- Чтение сообщения об ошибке
- Просмотр логов в окне
- Запись всех ошибок в файл

### 3. Улучшенная обработка исключений
✅ Все исключения ловятся и логируются в file + GUI

---

## Как просмотреть логи

### Способ 1: Автоматический скрипт
```bash
python view_splash_logs.py
```

Это отправит логи в консоль с форматированием.

### Способ 2: Вручную через File Explorer
1. Нажимите `Win + R`
2. Введите:
```
%APPDATA%\PANDORA\logs\
```
3. Откроется папка с логами
4. Откройте `splash.log` в текстовом редакторе

### Способ 3: Command Line (PowerShell)
```powershell
# Просмотр последних 50 строк
Get-Content "$env:APPDATA\PANDORA\logs\splash.log" -Tail 50

# Просмотр всех логов
type "$env:APPDATA\PANDORA\logs\splash.log"

# Фильтр по ошибкам
Select-String "ERROR" "$env:APPDATA\PANDORA\logs\splash.log"
```

---

## Тестирование перед сборкой EXE

### Запустите тестовый скрипт
```bash
python test_splash_logging.py
```

Это проверит:
- ✅ Загрузка модулей
- ✅ Создание splash screen
- ✅ Логирование разных типов сообщений
- ✅ Обновление прогресса
- ✅ Запись в файл логов
- ✅ Задержка при ошибке

### Ожидаемый результат
```
[TEST 1] Importing modules...
✓ splash_screen_pro imported successfully

[TEST 2] Creating splash screen and manager...
✓ Splash screen created
✓ Log file: C:\Users\...\AppData\Roaming\PANDORA\logs\splash.log

[TEST 3] Showing splash screen...
✓ Splash displayed

[TEST 4] Testing different log types...
✓ All log types tested

[TEST 5] Testing progress updates...
✓ Progress updates working

[TEST 6] Checking log file...
✓ Log file exists (XXXX bytes)

[TEST 7] Testing error delay (10 second wait)...
⚠ This will wait 10 seconds to test error_delay=True
```

---

## Что записывается в логи

### При успешном запуске
```
[10:30:45] [INFO] Backend listening on http://127.0.0.1:8000
[10:30:46] [SUCCESS] ✓ Loaded 1355 prompts from database
[10:30:47] [INFO] PyWebView initializing
[10:30:48] [STEP] ▶ Creating application window
[10:30:49] [SUCCESS] ✓ Window created successfully
```

### При ошибке backend
```
[10:30:45] [INFO] Starting Uvicorn server...
[10:30:46] [ERROR] ✗ Address already in use: 127.0.0.1:8000
[10:30:46] [ERROR] Traceback: OSError: [WinError 10048] Only one usage...
[10:30:46] [ERROR] Waiting 10 seconds before close...
```

### При ошибке PyWebView
```
[10:30:47] [STEP] ▶ Creating application window
[10:30:47] [ERROR] ✗ PyWebView initialization failed: ...
[10:30:47] [ERROR] Traceback: ImportError: No module named 'webview'
[10:30:47] [INFO] Логи сохранены: C:\Users\...\AppData\Roaming\PANDORA\logs\splash.log
```

---

## Структура логов

Каждая строка логов имеет формат:
```
[HH:MM:SS] [STATUS] Message
```

**STATUS типы:**
- `INFO` - информационное сообщение (синий)
- `SUCCESS` - успешное действие (зелёный)
- `WARNING` - предупреждение (оранжевый)
- `ERROR` - ошибка (красный)
- `STEP` - переход к следующему шагу (индиго)

---

## Решение распространённых проблем

### Проблема: Splash экран закрывается сразу
**Решение:**
1. Запустите `view_splash_logs.py` чтобы увидеть ошибки
2. Проверьте логи по пути: `%APPDATA%\PANDORA\logs\splash.log`
3. Ищите строки с `[ERROR]`

### Проблема: Логи не записываются в файл
**Решение:**
1. Проверьте права доступа на `%APPDATA%\PANDORA\`
2. Убедитесь, что папка существует и доступна для записи
3. Запустите `test_splash_logging.py` для диагностики

### Проблема: Backend не запускается
**Решение:**
1. Проверьте в логах `Address already in use`
2. Если порт занят, измените в launcher.py: `BACKEND_PORT = 9000`
3. Убедитесь, что никакой другой процесс не использует порт 8000

```bash
netstat -an | findstr :8000  # Windows
lsof -i :8000               # macOS/Linux
```

### Проблема: PyWebView не работает
**Решение:**
```bash
pip install --upgrade pywebview
```

Проверьте в логах:
```
[ERROR] PyWebView is required. Install: pip install pywebview
```

---

## Для разработчиков

### Как добавить логирование в код

```python
# В launcher.py или любом месте, где есть manager
manager.log_info("Описание действия")
manager.log_success("Успешно выполнено!")
manager.log_warning("Предупреждение!")
manager.log_error("Ошибка!")

# Обновить прогресс
manager.step(0, "Шаг 1", "Выполняем первый шаг")
```

### Как просмотреть логи программно

```python
from pathlib import Path

log_file = Path.home() / "AppData" / "Roaming" / "PANDORA" / "logs" / "splash.log"

if log_file.exists():
    with open(log_file, "r", encoding="utf-8") as f:
        logs = f.read()
    print(logs)
```

---

## Информация для технической поддержки

При обращении в поддержку предоставьте:
1. Файл логов: `%APPDATA%\PANDORA\logs\splash.log`
2. Версия Windows: `ver` в Command Prompt
3. Версия Python: `python --version`
4. Версия PANDORA: см. в окне splash screen

---

## Файлы для диагностики

| Файл | Назначение |
|------|-----------|
| `test_splash_logging.py` | Тестирование логирования |
| `view_splash_logs.py` | Просмотр логов |
| `splash.log` | Файл логов (создается автоматически) |
| `launcher.py` | Главный лаунчер (v4.1 с улучшениями) |
| `splash_screen_pro.py` | UI splash screen (с логированием) |

---

## Новые параметры

### splash_screen_pro.py

```python
# Путь логов (создается автоматически)
log_file = Path(os.environ.get("APPDATA", ".")) / "PANDORA" / "logs" / "splash.log"

# Закрытие с задержкой при ошибке
splash.close(error_delay=True)  # ждет 10 секунд и показывает путь к логам
splash.close(error_delay=False) # закрывается сразу
```

### launcher.py

```python
# Обработка ошибок с логированием
try:
    # код
except Exception as e:
    self._log(f"Error: {e}", "error")
    if self.splash:
        self.splash.close(error_delay=True)  # 10 сек задержка
    return False
```

---

## Статистика

- 📝 **Размер лога**: 5-50 KB в зависимости от сессии
- 📊 **Сохраняется**: Постоянно (+ новые логи с каждым запуском)
- 🔄 **Очистка**: Файл перезаписывается (сохраняя предыдущие сессии)
- ⚡ **Производительность**: ~0.1% CPU для логирования

---

## Примеры логов

### Успешный запуск
```
================================================================================
[2024-12-10 10:30:45] PANDORA Splash Screen Started
================================================================================
[10:30:45] [INFO] Initializing backend...
[10:30:46] [INFO] Uvicorn server starting on 127.0.0.1:8000
[10:30:47] [SUCCESS] ✓ Backend ready
[10:30:48] [INFO] Loading frontend...
[10:30:49] [SUCCESS] ✓ Frontend loaded
[10:30:50] [STEP] ▶ Creating window
[10:30:51] [SUCCESS] ✓ Application ready!
```

### Ошибка запуска
```
================================================================================
[2024-12-10 10:30:45] PANDORA Splash Screen Started
================================================================================
[10:30:45] [INFO] Initializing backend...
[10:30:46] [ERROR] ✗ Address already in use: 127.0.0.1:8000
[10:30:46] [ERROR] Traceback: OSError: [WinError 10048]...
[10:30:47] [ERROR] Ошибка при запуске. Закрытие через 10 секунд...
[10:30:47] [INFO] Логи сохранены: C:\Users\...\AppData\Roaming\PANDORA\logs\splash.log
```

---

**PANDORA v2.0.1 - Professional Logging & Diagnostics** 🔍
