# 🚀 PANDORA Launcher v4 - Splash Screen Integration

## Обновления v2.0.1 (Текущая версия)

### Что нового

#### 1. **Профессиональный Splash Screen**
- 🎨 Тёмная тема с акцентом индиго
- 📊 Прогресс-бар с процентами
- 📝 Логирование действий в реальном времени
- ✨ Плавные анимации загрузки

#### 2. **Интеграция с Backend**
```python
# Все логи backend теперь видны в splash screen
- Starting Backend Server
- Loading Frontend
- Creating Window
```

#### 3. **Улучшенная обработка ошибок**
- Перехват ошибок подключения
- Красивый вывод ошибок в splash
- Fallback на стандартный запуск без splash

#### 4. **Управление процессами**
- ✅ Одно окно (больше нет бесконечного цикла)
- ✅ Корректное завершение при Ctrl+C
- ✅ Чистая очистка ресурсов

---

## Архитектура

### Компоненты

```
┌─────────────────────────────────────────────┐
│  main() - Entry Point                       │
│  - Инициализирует splash screen             │
│  - Создает ApplicationLauncher               │
│  - Запускает приложение                      │
└──────────────┬──────────────────────────────┘
               │
       ┌───────▼────────┐
       │ Splash Screen  │
       │ (Tkinter)      │
       │                │
       │ - Progress Bar │
       │ - Log Output   │
       │ - Status Info  │
       └───────┬────────┘
               │
┌──────────────▼──────────────────────────────┐
│  AppLauncher                                │
│  - Backend (FastAPI + Uvicorn)             │
│  - Frontend (PyWebView)                     │
│  - Координация запуска                      │
└──────────────┬──────────────────────────────┘
               │
       ┌───────▼────────────┬──────────────┐
       │                    │              │
    ┌──▼──┐            ┌────▼──┐      ┌───▼──┐
    │ API │            │ UI    │      │ Logs │
    └─────┘            └───────┘      └──────┘
```

---

## Файлы системы

### Основные компоненты

**`desktop/launcher.py`** (Version 4 - 450+ строк)
- `UvicornBackend` класс - управление FastAPI/Uvicorn
- `AppLauncher` класс - координация приложения
- `main()` функция - entry point

**`desktop/splash_screen_pro.py`** (NEW - 220+ строк)
- `LoadingScreen` класс - UI splash screen
- `InitializationManager` класс - управление шагами загрузки

### Интеграционные точки

```python
# В launcher.py v4

# 1. Импорт splash module
try:
    from splash_screen_pro import create_splash_and_manager
    HAS_SPLASH = True
except ImportError:
    HAS_SPLASH = False

# 2. Глобальные переменные
_splash = None
_manager = None

# 3. UvicornBackend._log() - двойное логирование
def _log(self, message: str, status: str = "info"):
    if self.manager:
        getattr(self.manager, f'log_{status}')(message)
    else:
        logger.info(message)

# 4. AppLauncher.run() - использует splash
if self.splash:
    self.splash.show()
if self.manager:
    self.manager.step(0, "Starting Backend")

# 5. main() - создаёт splash
_splash, _manager = create_splash_and_manager()
launcher = AppLauncher(_splash, _manager)
```

---

## Использование

### Для разработчиков

```bash
# DEV режим (с отладкой)
python desktop/launcher.py

# Или через start_dev.py
python start_dev.py
```

### Для пользователей

```bash
# Запуск exe
./PANDORA_v2.0.exe
```

### Что видит пользователь

```
╔════════════════════════════════════════╗
║  PANDORA v2.0                          ║
║  Loading...                            ║
║                                        ║
║  ████████████░░░░░░░░░░░░░░░░  65%    ║
║                                        ║
║  [✓] Starting Backend Server            ║
║  [▶] Loading Frontend                   ║
║  [ ] Creating Window                    ║
║                                        ║
║  Initializing API and loading prompts   ║
╚════════════════════════════════════════╝
```

---

## Процесс запуска

### Этап 1: Инициализация (1-2 сек)
```
✓ Импорт модулей
✓ Проверка конфигурации
✓ Создание splash screen
✓ Инициализация менеджера загрузки
```

### Этап 2: Backend (3-5 сек)
```
▶ Запуск FastAPI
▶ Загрузка 1355 промптов из БД
▶ Инициализация хендлеров API
✓ Backend готов на 127.0.0.1:8000
```

### Этап 3: Frontend (1-2 сек)
```
▶ Подготовка UI компонентов
▶ Инициализация JavaScript
✓ Frontend готов
```

### Этап 4: Окно (0.5 сек)
```
▶ Создание PyWebView окна
▶ Загрузка страницы
✓ Приложение готово
```

**Итого: 5-10 секунд от клика до рабочего приложения**

---

## Обработка ошибок

### Сценарий 1: Backend не запускается
```
[✓] Starting Backend Server
[✗] ERROR: Address already in use port 8000
[Fallback] Trying alternate port...
```

### Сценарий 2: PyWebView не установлен
```
[✗] PyWebView is required
[INFO] Install: pip install pywebview
[Fallback] Opening browser: http://127.0.0.1:3000
```

### Сценарий 3: Splash screen не доступен
```
[⚠] Splash screen module not available
[Fallback] Starting without splash screen
[✓] Application started normally
```

---

## Параметры конфигурации

### `launcher.py`

```python
# Размеры окна
WINDOW_WIDTH = 1200
WINDOW_HEIGHT = 800
MIN_WIDTH = 900
MIN_HEIGHT = 600

# Порты
BACKEND_PORT = 8000
FRONTEND_PORT = 3000

# Таймауты
BACKEND_STARTUP_TIMEOUT = 20  # секунд
CONNECTION_RETRY_ATTEMPTS = 80
CONNECTION_RETRY_DELAY = 0.25  # секунд
```

### `splash_screen_pro.py`

```python
# Цвета темы
DARK_BG = "#1a1a2e"      # Темный фон
ACCENT_COLOR = "#6366f1" # Индиго

# Размеры
WINDOW_WIDTH = 500
WINDOW_HEIGHT = 400

# Шрифты
TITLE_FONT = ("Segoe UI", 18, "bold")
INFO_FONT = ("Courier New", 9, "normal")
```

---

## Запуск от разработчиков

### Для тестирования

```bash
# Terminal 1 - Backend
python start_dev.py

# Terminal 2 - Frontend (dev server)
cd frontend && npm start

# Откройте в браузере
http://127.0.0.1:3000
```

### Для сборки

```bash
# Собрать exe
python desktop/build_exe_final.py

# Результат: ./build/PANDORA/PANDORA_v2.0.exe
```

---

## Решение проблем

### Проблема: "ERR_CONNECTION_REFUSED"
**Решение:**
```bash
# Проверьте, что backend запустился
netstat -an | findstr :8000  # Windows
lsof -i :8000               # macOS/Linux

# Если port занят
python -c "import socket; s=socket.socket(); s.bind(('127.0.0.1',8000)); print('OK')"
```

### Проблема: Splash не закрывается
**Решение:**
```python
# Убедитесь, что в run() метод вызывает:
if self.splash:
    try:
        self.splash.close()
    except:
        pass
```

### Проблема: Логи не отображаются
**Решение:**
```python
# Проверьте, что manager передан:
launcher = AppLauncher(_splash, _manager)  # ✓ Правильно

launcher = AppLauncher()  # ✗ Неправильно (нет логов)
```

---

## Тестирование

### Unit тесты

```bash
# Backend tests
cd backend && pytest tests/ -v

# Frontend tests  
cd frontend && npm test
```

### Интеграционные тесты

```bash
# Полный цикл запуска
python -c "
from desktop.launcher import main
main()
"

# Проверка splash screen
python -c "
from desktop.splash_screen_pro import create_splash_and_manager
splash, manager = create_splash_and_manager()
splash.show()
manager.step(0, 'Test Step')
import time; time.sleep(3)
splash.close()
"
```

---

## Производительность

### Метрики запуска

| Компонент | Время | Примечание |
|-----------|-------|-----------|
| Импорт модулей | 0.5s | Python инициализация |
| Создание splash | 0.3s | Tkinter инициализация |
| Backend startup | 3-5s | FastAPI + Uvicorn + DB |
| Frontend load | 1-2s | PyWebView инициализация |
| UI render | 0.5s | Отрисовка HTML/CSS |
| **ИТОГО** | **5-10s** | В зависимости от ПК |

### Использование ресурсов

- **RAM**: ~150-200 MB (во время работы)
- **CPU**: ~30-50% (во время запуска)
- **Disk**: 39.6 MB (exe размер)

---

## История версий

### v2.0.1 (Текущая)
- ✨ Новый splash screen с логированием
- 🔧 Интеграция с launcher v4
- 📊 Прогресс-бар с анимацией
- 🎨 Профессиональный UI

### v2.0.0
- ✅ Полная рефакторизация проекта
- 🔥 Удалены 12 старых файлов
- 🐛 Исправлена ошибка бесконечного цикла окон

### v1.0.3
- 🚀 Первый стабильный релиз
- 📦 PyInstaller поддержка

---

## Заметки разработчика

### Для будущих обновлений

1. **Асинхронность**: Consider using async/await для backend
2. **Прогрессивная загрузка**: Load prompts по мере необходимости
3. **Кеширование**: Cache frontend assets для быстрого запуска
4. **Web Workers**: Перенести backend processing в worker thread
5. **Service Worker**: Add offline support

### Для продвинутых пользователей

```python
# Переопределить конфиг
import desktop.launcher as launcher
launcher.BACKEND_PORT = 9000
launcher.WINDOW_WIDTH = 1600

# Запустить с собственными параметрами
launcher.main()
```

---

## Контакты поддержки

- 📝 **Документация**: See `/docs/` folder
- 🐛 **Баги**: GitHub Issues
- 💬 **Вопросы**: GitHub Discussions
- 📧 **Email**: support@pandora.local

---

**PANDORA v2.0.1 - Professional Prompt Manager with Beautiful Loading Experience** ✨
