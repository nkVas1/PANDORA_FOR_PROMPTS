# PANDORA v2.0 - Инструкция по сборке и развертыванию

## 📋 Оглавление
1. [Локальный запуск](#локальный-запуск)
2. [Сборка Windows EXE](#сборка-windows-exe)
3. [Создание установщика](#создание-установщика)
4. [Развертывание](#развертывание)
5. [Устранение проблем](#устранение-проблем)

---

## Локальный запуск

### Быстрый старт (30 секунд)

```powershell
# 1. Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# 2. Запустите приложение
python start_v2.py
```

**Результат:**
- ✅ API запустится на `http://127.0.0.1:8000`
- ✅ Браузер откроется автоматически
- ✅ Нажмите `Ctrl+C` для остановки

### Параметры запуска

```powershell
# Только API (без открытия браузера)
python start_v2.py --api-only

# Только открыть приложение в браузере
python start_v2.py --browser-only

# Справка
python start_v2.py --help
```

### Что происходит при запуске?

```
┌─ Проверка окружения ─────────────────┐
│ ✓ Файл .env                          │
│ ✓ Зависимости Python                 │
│ ✓ Свободные порты                    │
└──────────────────────────────────────┘
          ↓
┌─ Запуск компонентов ─────────────────┐
│ 🌐 FastAPI на http://127.0.0.1:8000  │
│ 📂 Database: pandora.db              │
│ 🎨 Frontend: HTML5 + CSS v2.0        │
└──────────────────────────────────────┘
          ↓
┌─ Открытие приложения ────────────────┐
│ 🌐 Браузер → http://127.0.0.1:8000   │
│ ✨ Готово к работе                   │
└──────────────────────────────────────┘
```

---

## Сборка Windows EXE

### Требования

```
✓ Python 3.8+
✓ PyInstaller 6.17.0+
✓ Все зависимости установлены
✓ Windows 7+ или выше
✓ 2+ GB свободного место на диске
```

### Полная сборка (рекомендуется)

```powershell
# 1. Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# 2. Запустите скрипт сборки
python build_exe_v2.py

# Это сделает:
# ✓ Проверит окружение
# ✓ Очистит старые артефакты
# ✓ Запустит PyInstaller
# ✓ Протестирует результат
# ✓ Создаст BUILD_REPORT_v2.0.md
```

**Время сборки:** 5-10 минут в зависимости от ПК

### Быстрая сборка (для повторных попыток)

```powershell
python build_exe_v2.py --quick

# Пропустит очистку старых артефактов
# Ускорит процесс на 30-40%
```

### Только очистка артефактов

```powershell
python build_exe_v2.py --clean

# Удалит: build/, dist/, __pycache__
# Полезно перед чистой сборкой
```

### Тестирование собранного EXE

```powershell
python build_exe_v2.py --test

# Проверит:
# ✓ Наличие файла PANDORA_v2.0.exe
# ✓ Размер EXE (< 500 MB)
# ✓ Валидный Windows PE формат
```

### Результаты сборки

После успешной сборки в корне проекта появятся:

```
PANDORA_v2.0.exe          (≈150-200 MB)
BUILD_REPORT_v2.0.md      (отчет о сборке)
dist/PANDORA/             (распакованная версия)
```

### Запуск собранного EXE

```powershell
# Просто дважды щелкните на PANDORA_v2.0.exe

# ИЛИ из PowerShell:
.\PANDORA_v2.0.exe

# Приложение автоматически:
# ✓ Создаст локальное окружение
# ✓ Запустит FastAPI сервер
# ✓ Откроет браузер
# ✓ Инициализирует БД
```

### Что делает PyInstaller?

```
Входные данные:
├─ launcher_final.py      (точка входа)
├─ backend/               (API)
├─ frontend/              (HTML/CSS/JS)
├─ requirements.txt       (зависимости)
└─ PANDORA.spec          (конфиг)
        ↓
    PyInstaller
        ↓
Выходной файл:
└─ PANDORA_v2.0.exe       (≈200 MB)
   ├─ Python runtime
   ├─ Все зависимости
   ├─ Backend + Frontend
   └─ База данных (SQLite)
```

---

## Создание установщика

### Установка NSIS

```powershell
# 1. Скачайте NSIS с https://nsis.sourceforge.io/
# 2. Установите (по умолчанию: C:\Program Files\NSIS)
# 3. Проверьте:
C:\Program Files\NSIS\makensis.exe --version
```

### Создание скрипта установщика

Создайте файл `PANDORA_Installer.nsi`:

```nsis
; PANDORA v2.0 - NSIS Installer Script
; Сохраните в корне проекта

!include "MUI2.nsh"

; ==================== КОНСТАНТЫ ====================
!define APPNAME "PANDORA"
!define APPVERSION "2.0.0"
!define APPNAMEANDVERSION "${APPNAME} v${APPVERSION}"
!define COMPANYNAME "PANDORA Team"
!define APPEXE "PANDORA_v2.0.exe"
!define INSTALLDIR "$PROGRAMFILES\${APPNAME}"

; ==================== ПЕРЕМЕННЫЕ ====================
Name "${APPNAMEANDVERSION}"
OutFile "PANDORA_v2.0_Setup.exe"
InstallDir "${INSTALLDIR}"
InstallDirRegKey HKLM "Software\${APPNAME}" "Install_Dir"

; ==================== ИНТЕРФЕЙС ====================
!insertmacro MUI_PAGE_DIRECTORY
!insertmacro MUI_PAGE_INSTFILES
!insertmacro MUI_LANGUAGE "English"

; ==================== УСТАНОВКА ====================
Section "Install"
    SetOutPath "$INSTDIR"
    
    ; Копировать основной EXE
    File "PANDORA_v2.0.exe"
    
    ; Копировать остальное
    File /r "dist\PANDORA\*.*"
    
    ; Создать ярлыки
    CreateDirectory "$SMPROGRAMS\${APPNAME}"
    CreateShortcut "$SMPROGRAMS\${APPNAME}\${APPNAME}.lnk" "$INSTDIR\${APPEXE}"
    CreateShortcut "$DESKTOP\${APPNAME}.lnk" "$INSTDIR\${APPEXE}"
    
    ; Реестр
    WriteRegStr HKLM "Software\${APPNAME}" "Install_Dir" "$INSTDIR"
    WriteRegStr HKLM "Software\Microsoft\Windows\CurrentVersion\Uninstall\${APPNAME}" "DisplayName" "${APPNAMEANDVERSION}"
    WriteRegStr HKLM "Software\Microsoft\Windows\CurrentVersion\Uninstall\${APPNAME}" "UninstallString" "$INSTDIR\Uninstall.exe"
    
    ; Создать Uninstall.exe
    WriteUninstaller "$INSTDIR\Uninstall.exe"
SectionEnd

; ==================== УДАЛЕНИЕ ====================
Section "Uninstall"
    RMDir /r "$INSTDIR"
    RMDir /r "$SMPROGRAMS\${APPNAME}"
    Delete "$DESKTOP\${APPNAME}.lnk"
    DeleteRegKey HKLM "Software\${APPNAME}"
    DeleteRegKey HKLM "Software\Microsoft\Windows\CurrentVersion\Uninstall\${APPNAME}"
SectionEnd
```

### Сборка установщика

```powershell
# 1. Убедитесь, что PANDORA_v2.0.exe собран
# 2. Создайте скрипт установщика (PANDORA_Installer.nsi)
# 3. Запустите:

& "C:\Program Files\NSIS\makensis.exe" "PANDORA_Installer.nsi"

# Результат: PANDORA_v2.0_Setup.exe (≈100 MB)
```

### Распространение установщика

```powershell
# Отправьте пользователям:
# 📦 PANDORA_v2.0_Setup.exe

# Пользователь:
# 1. Запускает Setup.exe
# 2. Следует инструкциям установщика
# 3. Нажимает "Install"
# 4. Приложение готово к использованию
```

---

## Развертывание

### Для одного пользователя

```
1. Скачайте PANDORA_v2.0.exe или Setup.exe
2. Дважды щелкните
3. Приложение готово

✓ Требуется: .NET Framework (обычно есть на Windows)
✓ Не требуется: Python, Virtual Environment, установка зависимостей
```

### Для нескольких пользователей

```
1. Создайте сетевую папку или облачное хранилище
2. Разместите PANDORA_v2.0.exe или Setup.exe
3. Отправьте ссылку пользователям
4. Каждый пользователь скачивает и запускает локально
```

### Автоматизированное развертывание

```powershell
# Развертывание с Group Policy (для корпоративной сети)
# 1. Поместите Setup.exe на GPO файл-сервер
# 2. Создайте Group Policy для установки
# 3. Приложение установится на все ПК в группе

# Или используйте Microsoft Intune / SCCM
```

---

## Структура скомпилированного EXE

```
PANDORA_v2.0.exe  (≈200 MB)
│
├─ Python Runtime
│  ├─ python3.11.dll
│  └─ (все встроенные модули)
│
├─ Dependencies (все pip пакеты)
│  ├─ fastapi/
│  ├─ uvicorn/
│  ├─ sqlalchemy/
│  ├─ pydantic/
│  └─ (51 других пакет)
│
├─ Backend (FastAPI)
│  ├─ app/main.py
│  ├─ api/routes.py
│  ├─ models/
│  └─ services/
│
├─ Frontend (v2.0 Design)
│  ├─ index.html
│  ├─ css/
│  │  ├─ design-system.css
│  │  ├─ components.css
│  │  ├─ utilities.css
│  │  └─ animations.css
│  ├─ js/
│  │  └─ app.js (theme, ui managers)
│  └─ images/
│
├─ Database
│  └─ pandora.db (SQLite)
│
└─ Configuration
   ├─ .env (если есть)
   └─ (другие конфиги)
```

---

## Параметры запуска EXE

### Из командной строки

```powershell
# Обычный запуск
.\PANDORA_v2.0.exe

# С логами (выведет в консоль)
.\PANDORA_v2.0.exe --verbose

# Указать кастомный порт (если поддерживается)
.\PANDORA_v2.0.exe --port 9000
```

### Ярлык на рабочем столе

```
Создайте ярлык с параметрами:
Цель: C:\Users\User\PANDORA_v2.0.exe
Начальная папка: C:\Users\User\
```

---

## Устранение проблем

### "Python не найден при запуске start_v2.py"

```powershell
# Решение: Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# ИЛИ используйте полный путь
c:\Users\YourName\PANDORA_FOR_PROMPTS\venv\Scripts\python.exe start_v2.py
```

### "Порт 8000 занят"

```powershell
# Найдите процесс, использующий порт
Get-NetTCPConnection -LocalPort 8000 | Stop-Process -Force

# ИЛИ используйте другой порт
python start_v2.py --port 9000
```

### "PyInstaller не найден"

```powershell
pip install PyInstaller==6.17.0
python build_exe_v2.py
```

### "EXE не запускается после сборки"

```powershell
# 1. Проверьте, что сборка прошла без ошибок
python build_exe_v2.py --test

# 2. Проверьте логи
Get-Content "build_output.log" | tail -50

# 3. Попробуйте пересборку с очисткой
python build_exe_v2.py --clean
python build_exe_v2.py
```

### "Ошибка при импорте в EXE"

```
Вероятная причина: Некоторые импорты не включены в PANDORA.spec

Решение:
1. Откройте PANDORA.spec
2. Добавьте модуль в hiddenimports:
   hiddenimports += ['your_module']
3. Пересоберите:
   python build_exe_v2.py --clean
```

### "NSIS не найден при создании Setup.exe"

```powershell
# Убедитесь, что NSIS установлен
# Скачайте с: https://nsis.sourceforge.io/

# Проверьте путь
ls "C:\Program Files\NSIS\"

# Если нет, измените путь в скрипте
```

---

## Оптимизация размера EXE

### Текущий размер: ≈200 MB

Если нужно уменьшить:

```python
# В PANDORA.spec измените:

# 1. Отключить отладку
exe = EXE(..., debug=False, ...)

# 2. Включить UPX сжатие (если установлен)
exe = EXE(..., upx=True, ...)

# 3. Исключить ненужные модули
excludes=['matplotlib', 'numpy', 'pandas', ...],

# 4. Перестроить
python build_exe_v2.py --clean
```

---

## Требования пользователя к системе

| Требование | Минимум | Рекомендуется |
|-----------|---------|---------------|
| Windows | 7 SP1 | 10 / 11 |
| RAM | 2 GB | 4+ GB |
| Диск | 250 MB | 500 MB |
| .NET | Framework 4.5+ | 6.0+ |
| Интернет | Не требуется | Опционально |

---

## Версионирование

```
PANDORA_v2.0.exe          # Текущая версия
PANDORA_v1.9.exe          # Предыдущая версия
PANDORA_v2.0_Setup.exe    # Установщик
```

Для обновления версии:
1. Обновите `Config.APP_VERSION` в `build_exe_v2.py`
2. Обновите `CHANGELOG.md`
3. Пересоберите: `python build_exe_v2.py`

---

## Полезные ссылки

- 📚 [PyInstaller Документация](https://pyinstaller.org/)
- 📚 [NSIS Документация](https://nsis.sourceforge.io/)
- 📚 [FastAPI Документация](https://fastapi.tiangolo.com/)
- 🔗 [GitHub Репозиторий](https://github.com/your-username/pandora)

---

## Контакты и поддержка

Если у вас возникли проблемы:

1. Проверьте `TROUBLESHOOTING.md`
2. Посмотрите логи в `build_output.log`
3. Создайте Issue на GitHub
4. Свяжитесь: support@pandora.app

---

**Последнее обновление:** 2025-01-01  
**Версия документации:** 2.0.0  
**Статус:** ✅ Готово к использованию
