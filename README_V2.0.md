# PANDORA v2.0 - Professional Prompt Manager

> Полнофункциональное настольное приложение для управления промптами с красивым UI, аналитикой и интеграцией с OpenAI

## 🚀 Быстрый старт

### Вариант 1: Запуск готового EXE (Рекомендуется)

1. **Собрать EXE**:
   ```bash
   python build_exe_final.py
   ```

2. **Запустить приложение**:
   ```bash
   .\dist\PANDORA\PANDORA.exe
   ```

### Вариант 2: Разработка (Python)

1. **Установить зависимости**:
   ```bash
   pip install -r backend/requirements.txt
   ```

2. **Запустить через quickstart**:
   ```bash
   python quickstart.py
   ```

3. **Выбрать режим**:
   - `1` - Full App (Desktop + Splash Screen)
   - `2` - Backend Only (Uvicorn)
   - `3` - Frontend Only (HTTP Server)

## 📋 Требования

- **Python**: 3.10 или выше
- **OS**: Windows, macOS, Linux
- **RAM**: 512 MB минимум
- **Disk**: 200 MB для EXE

## 🎨 Особенности

### 🎯 Dashboard
- Статистика промптов, проектов, тегов
- Недавние промпты с счетчиком использования
- Метрики роста (7-дневный тренд)
- Быстрые действия (новый промпт, новый проект)
- Рекомендации AI

### 📝 Управление промптами
- Создание, редактирование, удаление промптов
- Поиск и фильтрация по категориям
- Сортировка по названию, дате, популярности
- Поддержка Markdown
- Вставка переменных (шаблоны)
- AI оптимизация (улучшение промптов)
- Живой предпросмотр

### 📊 Аналитика
- Статистика использования промптов
- Анализ популярности
- Тренды по времени
- Экспорт данных
- Визуализация категорий

### 🔨 Проекты
- Организация промптов в проекты
- Отслеживание прогресса
- Связывание промптов с проектами
- Grid/List представления

### ⌨️ Навигация
- **Cmd+K** - Командная палитра (быстрый поиск команд)
- **Tab** - Переход между элементами
- **Enter** - Выбрать элемент
- **Escape** - Закрыть модальное окно

## 🏗️ Архитектура

### Backend
```
backend/
  ├── app/
  │   ├── main.py           # FastAPI приложение
  │   ├── config.py         # Конфигурация
  │   ├── db/
  │   │   ├── database.py   # SQLAlchemy setup
  │   │   ├── models.py     # ORM модели
  │   │   └── schemas.py    # Pydantic схемы
  │   └── routes/
  │       ├── prompts.py
  │       ├── projects.py
  │       ├── analytics.py
  │       └── ...
  └── requirements.txt
```

### Frontend
```
frontend/
  ├── index.html            # Главный файл
  ├── src/
  │   ├── core/             # Ядро приложения
  │   │   ├── app.js        # Инициализация
  │   │   ├── router.js     # Маршрутизация
  │   │   └── state-manager.js  # Управление состоянием
  │   ├── views/            # Экраны приложения
  │   │   ├── Dashboard.js
  │   │   ├── PromptsView.js
  │   │   ├── EditorView.js
  │   │   ├── ProjectsView.js
  │   │   └── AnalyticsView.js
  │   ├── components/       # Компоненты UI
  │   │   ├── Card.js
  │   │   ├── Button.js
  │   │   ├── CommandPalette.js
  │   │   └── PromptEditor.js
  │   ├── utils/            # Утилиты
  │   │   ├── http.js       # HTTP клиент
  │   │   └── animated-background.js
  │   └── css/              # Стили
  │       ├── tokens.css    # Дизайн система
  │       ├── components.css
  │       ├── views.css
  │       ├── animations.css
  │       └── dashboard.css
```

### Desktop
```
desktop/
  ├── launcher.py           # Главный запускатель
  └── splash_screen_v3.py   # Экран загрузки
```

## 📁 Структура проекта

```
PANDORA_FOR_PROMPTS/
├── backend/              # FastAPI backend
├── frontend/             # Vanilla JS frontend
├── desktop/              # Desktop integration
├── data/                 # Данные приложения
├── docs/                 # Документация
├── build_exe_final.py    # Сборка EXE
├── quickstart.py         # Быстрый запуск
├── health_check.py       # Проверка здоровья
└── README.md
```

## 🔌 API Endpoints

### Prompts
- `GET /api/prompts` - Список всех промптов
- `POST /api/prompts` - Создать новый
- `GET /api/prompts/{id}` - Получить один
- `PUT /api/prompts/{id}` - Обновить
- `DELETE /api/prompts/{id}` - Удалить
- `POST /api/prompts/optimize` - AI оптимизация

### Analytics
- `GET /api/analytics/dashboard` - Данные дашборда
- `GET /api/analytics/prompts` - Аналитика промптов
- `GET /api/analytics/growth` - Метрики роста
- `POST /api/analytics/export` - Экспорт данных

### Projects
- `GET /api/projects` - Список проектов
- `POST /api/projects` - Создать
- `GET /api/projects/{id}` - Получить
- `PUT /api/projects/{id}` - Обновить
- `DELETE /api/projects/{id}` - Удалить

## 🎯 Технологический стек

### Backend
- **FastAPI** - Web framework
- **SQLAlchemy 2.0** - ORM
- **Pydantic v2** - Валидация данных
- **Uvicorn** - ASGI сервер
- **SQLite** - База данных
- **OpenAI API** - AI интеграция

### Frontend
- **Vanilla JavaScript** (ES6+)
- **CSS3** (Grid, Flexbox, Animations)
- **Canvas API** - Анимированный фон
- **LocalStorage** - Персистентность

### Desktop
- **PyWebView** - Браузер (Chromium/WebKit)
- **Tkinter** - Экран загрузки
- **PyInstaller** - Упаковка в EXE
- **Uvicorn** - Встроенный сервер

## 🧪 Тестирование

### Проверка целостности
```bash
python health_check.py
```

### Запуск тестов
```bash
cd backend
pytest
```

### Сборка и тестирование
```bash
python build_and_test.py
```

## 📦 Сборка релиза

```bash
# Полная сборка с тестами
python build_and_test.py

# Или вручную
python build_exe_final.py

# Результат в
./dist/PANDORA/PANDORA.exe
```

## 🔧 Конфигурация

### Backend (.env)
```
DATABASE_URL=sqlite:///./pandora.db
OPENAI_API_KEY=sk-xxx
DEBUG=False
```

### Frontend
Конфигурация находится в `frontend/src/core/app.js`:
```javascript
window.http = new HTTPClient('http://127.0.0.1:8000/api');
```

## 🐛 Troubleshooting

### EXE не запускается
- Проверить логи в `dist/PANDORA/logs/splash.log`
- Убедиться что Python 3.10+ установлен
- Пересобрать: `python build_exe_final.py`

### Frontend не загружается
- Проверить пути импорта в `index.html`
- Очистить LocalStorage (DevTools → Application)
- Проверить console в DevTools

### Backend не отвечает
- Проверить что порт 8000 свободен
- Проверить логи splash screen
- Запустить backend отдельно: `python quickstart.py` → режим 2

### БД ошибки
- Удалить `pandora.db`: `rm *.db`
- Пересоздать: приложение создаст автоматически

## 📊 Файловая система

### Логи приложения
```
dist/PANDORA/logs/splash.log  # Логи запуска
```

### База данных
```
pandora.db  # SQLite БД
```

### Данные приложения
```
data/
  ├── prompts/     # Сохраненные промпты
  ├── projects/    # Проекты
  └── imports/     # Импортированные файлы
```

## 📝 Лицензия

MIT License - используйте свободно

## 🤝 Контрибьюции

Предложения и улучшения приветствуются!

## 📞 Поддержка

- 📖 Документация в `docs/`
- 🐛 Сообщайте об ошибках через GitHub Issues
- 💡 Идеи в Discussions

## 🎉 Спасибо!

Спасибо за использование PANDORA v2.0!
Если вам нравится приложение, поставьте ⭐ на GitHub!

---

**Версия**: 2.0.0  
**Дата**: 2024  
**Статус**: Production Ready ✅
